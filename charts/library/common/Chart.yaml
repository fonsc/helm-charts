---
apiVersion: v2
name: common
description: Function library for Helm charts
type: library
version: 3.6.1
kubeVersion: ">=1.22.0-0"
keywords:
  - common
  - library
home: https://github.com/fonsc/helm-charts/tree/main/charts/library/common
maintainers:
  - name: fonsc
    email: me@fonsc.dev
annotations:
  artifacthub.io/changes: |-
    - kind: fixed
      description: |-
        Only add hostUsers when explicitly configured
    - kind: fixed
      description: |-
        `hosts` should be an optional field for Ingress resources
    - kind: fixed
      description: |-
        `defaultBackend` should be an object for Ingress resources
